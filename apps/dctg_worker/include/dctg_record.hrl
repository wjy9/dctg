-record(launcher,
    {
    intensity, % launch client per ms
    count, % total count of clients
    dest,
    interval, % scheduled time interval
    start_time,
    content,
    fraction,
    round
    }).